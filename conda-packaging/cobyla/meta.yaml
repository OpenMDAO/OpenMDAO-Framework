package:
    name: cobyla
    version: dev

source:
    path: ../../contrib/cobyla

build:
    preserve_egg_dir: yes
    
requirements:
    build:
        - python
        - setuptools
        - numpy

    run:
        - python
        - numpy
