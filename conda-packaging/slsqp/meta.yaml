package:
    name: slsqp
    version: dev

source:
    path: ../../contrib/slsqp

build:
    preserve_egg_dir: True
    
requirements:
    build:
        - python
        - setuptools
        - numpy
    
    run:
        - python
        - numpy
