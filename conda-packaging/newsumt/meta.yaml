package:
    name: newsumt
    version: !!str {{environ['PKG_VERSION']}}

source:
    path: ../../contrib/newsumt

build:
    preserve_egg_dir: True

requirements:
    build:
        - python
        - setuptools
        - numpy
        - libgfortran #[osx]

    run:
        - python
        - numpy
        - libgfortran #[osx]
