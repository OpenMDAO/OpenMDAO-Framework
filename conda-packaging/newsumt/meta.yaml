package:
    name: newsumt
    version: dev

source:
    path: ../../contrib/newsumt

build:
    preserve_egg_dir: yes
    
requirements:
    build:
        - python
        - setuptools
        - numpy

    run:
        - python
        - numpy
