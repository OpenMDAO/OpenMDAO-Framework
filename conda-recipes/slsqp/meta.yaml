package:
    name: slsqp
    version: !!str {{ environ['PKG_VERSION'] }}

source:
    path: ../../contrib/slsqp

build:
    preserve_egg_dir: True

requirements:
    build:
        - python
        - setuptools
        - numpy

    run:
        - python
        - numpy
